<form method="post" action="{{ path('payment_mode_delete', {'id': payment_mode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_mode.id) }}">
    <button class="btn-floating pink"><i class="material-icons">delete</i></button>
</form>
